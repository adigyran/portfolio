<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    >

    <ImageView
        android:id="@+id/fieldIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <TextView
        android:id="@+id/fieldText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:alpha="0.6"
        android:gravity="start"
        android:text="Speciality"
        android:textAppearance="@style/TextAppearance.App.Body.DescriptionText"
        app:layout_constraintEnd_toStartOf="@+id/fieldArrow"
        app:layout_constraintStart_toEndOf="@id/fieldIcon"
        app:layout_constraintTop_toTopOf="@id/fieldIcon"
        app:layout_constraintBottom_toBottomOf="@id/fieldIcon"
        />

    <ImageView
        android:id="@+id/fieldArrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_arrow_right"
        app:layout_constraintTop_toTopOf="@id/fieldIcon"
        app:layout_constraintBottom_toBottomOf="@id/fieldIcon"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>