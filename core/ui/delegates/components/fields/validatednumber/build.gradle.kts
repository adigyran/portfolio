plugins {
    id("healthapp.android.delegate")
}

android {
    namespace = "com.aya.digital.core.ui.delegates.components.fields.validatednumber"

    buildFeatures {
        viewBinding = true
    }
}
