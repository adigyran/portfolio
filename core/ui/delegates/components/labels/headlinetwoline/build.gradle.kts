plugins {
    id("healthapp.android.delegate")
}

android {
    namespace = "com.aya.digital.core.ui.delegates.components.labels.headlinetwoline"

    buildFeatures {
        viewBinding = true
    }
}
